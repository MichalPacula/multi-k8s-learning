sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mpac60/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true mpac60/react-test npm test

after_success:
  - docker build -t mpac60/multi-client ./client
  - docker build -t mpac60/multi-nginx ./nginx
  - docker build -t mpac60/multi-server ./server
  - docker build -t mpac60/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mpac60/multi-client
  - docker push mpac60/multi-nginx
  - docker push mpac60/multi-server
  - docker push mpac60/multi-worker
